"use client";
import {useState} from "react";
type Request={title:string;meta:string;votes:number;tint:string;icon:string};
export function RequestCard({request}:{request:Request}){const[votes,setVotes]=useState(request.votes);const[voted,setVoted]=useState(false);return <div className="flex items-center gap-4 rounded-2xl border border-[var(--line)] bg-[var(--cream)] p-4"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl text-sm font-bold" style={{backgroundColor:request.tint}}>{request.icon}</div><div className="min-w-0 flex-1"><h3 className="truncate text-sm font-bold sm:text-base">{request.title}</h3><p className="mt-1 text-xs text-[var(--muted)]">{request.meta}</p></div><button onClick={()=>{setVoted(!voted);setVotes(v=>voted?v-1:v+1)}} className={`flex min-w-[54px] flex-col items-center rounded-xl border px-3 py-2 text-xs font-bold ${voted?"border-[var(--maroon)] bg-[var(--maroon)] text-white":"border-[var(--line)] text-[var(--maroon)]"}`}><span className="text-sm">↑</span>{votes}</button></div>}
